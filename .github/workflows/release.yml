name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v1.5.10)'
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
    - name: Resolve version
      id: version
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "tag=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
        else
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        fi

    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        ref: ${{ steps.version.outputs.tag }}

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'

    - name: Run tests
      run: go test -v ./...

    - name: Build all platforms
      run: make build-all

    - name: Create checksums
      run: |
        cd dist
        sha256sum * > checksums.txt
        cat checksums.txt

    - name: Extract release notes
      id: extract-release-notes
      run: |
        VERSION=${{ steps.version.outputs.tag }}
        echo "version=${VERSION}" >> $GITHUB_OUTPUT

        # Extract changelog section for this version
        if [ -f CHANGELOG.md ]; then
          # Get content between this version and next version heading
          CHANGELOG=$(sed -n "/## \[${VERSION#v}\]/,/## \[/p" CHANGELOG.md | sed '$d' | tail -n +2)

          if [ -n "$CHANGELOG" ]; then
            echo "Found changelog for ${VERSION}"
            # Save to file for body_path
            echo "$CHANGELOG" > /tmp/changelog_section.md
            echo "" >> /tmp/changelog_section.md
            echo "has_changelog=true" >> $GITHUB_OUTPUT
          else
            echo "No changelog section found for ${VERSION}"
            echo "has_changelog=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "CHANGELOG.md not found"
          echo "has_changelog=false" >> $GITHUB_OUTPUT
        fi

        # Always create installation instructions
        cat >> /tmp/changelog_section.md << EOF
        ## Installation

        Download the appropriate binary for your platform:

        ### macOS (Apple Silicon)
        ```bash
        wget https://github.com/${{ github.repository }}/releases/download/${VERSION}/cc-api-proxy-darwin-arm64
        chmod +x cc-api-proxy-darwin-arm64
        sudo mv cc-api-proxy-darwin-arm64 /usr/local/bin/cc-api-proxy
        ```

        ### macOS (Intel)
        ```bash
        wget https://github.com/${{ github.repository }}/releases/download/${VERSION}/cc-api-proxy-darwin-amd64
        chmod +x cc-api-proxy-darwin-amd64
        sudo mv cc-api-proxy-darwin-amd64 /usr/local/bin/cc-api-proxy
        ```

        ### Linux (x86_64)
        ```bash
        wget https://github.com/${{ github.repository }}/releases/download/${VERSION}/cc-api-proxy-linux-amd64
        chmod +x cc-api-proxy-linux-amd64
        sudo mv cc-api-proxy-linux-amd64 /usr/local/bin/cc-api-proxy
        ```

        ### Linux (ARM64)
        ```bash
        wget https://github.com/${{ github.repository }}/releases/download/${VERSION}/cc-api-proxy-linux-arm64
        chmod +x cc-api-proxy-linux-arm64
        sudo mv cc-api-proxy-linux-arm64 /usr/local/bin/cc-api-proxy
        ```

        ### Windows
        Download `cc-api-proxy-windows-amd64.exe` and add to your PATH.

        ## Verify Installation
        ```bash
        cc-api-proxy --help
        ```

        ## Quick Start
        See [README.md](https://github.com/${{ github.repository }}#quick-start) for configuration instructions.
        EOF

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/cc-api-proxy-*
          dist/checksums.txt
        body_path: /tmp/changelog_section.md
        generate_release_notes: ${{ steps.extract-release-notes.outputs.has_changelog != 'true' }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
